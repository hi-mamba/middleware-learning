dependencies {

    // 获取的是本地的服务，因此你需要把这个JAR 上传到本地,
    compile group: 'space.mamba.mamba.service.register.discovery.example', name: 'register-core', version: '1.0-SNAPSHOT'
}

// 注意配置这里
bootJar { enabled = true }

bootJar {
    mainClassName = 'space.service.client.MambaServiceApplication'
}

